import { prisma } from '@/lib/prisma';
import bcrypt from 'bcryptjs';
import { registerSchema } from '@/lib/schema';
import { signIn } from '@/lib/auth';
async function registerAction(formData: FormData) { 'use server'; const parsed = registerSchema.parse({ name: formData.get('name'), email: formData.get('email'), password: formData.get('password'), organizationName: formData.get('organizationName'), slug: formData.get('slug') }); const passwordHash = await bcrypt.hash(parsed.password, 10); const user = await prisma.user.create({ data: { name: parsed.name, email: parsed.email, passwordHash, memberships: { create: { role: 'OWNER', organization: { create: { name: parsed.organizationName, slug: parsed.slug, regionCode: 'PL' } } } } } }); await signIn('credentials', { email: user.email, password: parsed.password, redirectTo: '/dashboard' }); }
async function loginAction(formData: FormData) { 'use server'; await signIn('credentials', { email: String(formData.get('email') || ''), password: String(formData.get('password') || ''), redirectTo: '/dashboard' }); }
export default function LoginPage() { return <main className="container"><h1 className="title">Logowanie i rejestracja</h1><div className="grid grid-2" style={{ marginTop: 24 }}><form action={loginAction} className="card section"><h2>Logowanie</h2><div><label>Email</label><input name="email" type="email" required /></div><div style={{marginTop:12}}><label>Hasło</label><input name="password" type="password" required /></div><button className="btn btn-primary" type="submit" style={{marginTop:16}}>Zaloguj</button></form><form action={registerAction} className="card section"><h2>Rejestracja tenant ownera</h2><div><label>Imię i nazwisko</label><input name="name" required /></div><div style={{marginTop:12}}><label>Email</label><input name="email" type="email" required /></div><div style={{marginTop:12}}><label>Hasło</label><input name="password" type="password" required /></div><div style={{marginTop:12}}><label>Nazwa organizacji</label><input name="organizationName" required /></div><div style={{marginTop:12}}><label>Slug organizacji</label><input name="slug" required /></div><button className="btn btn-primary" type="submit" style={{marginTop:16}}>Utwórz konto i tenant</button></form></div></main>; }